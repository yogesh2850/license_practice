window.APP_CONFIG = {
  // Leave empty to use this same site (/api) when you run `npm start`.
  // For GitHub Pages, set this to the public URL of the Node server, with no trailing slash.
  apiUrl: "",
};
